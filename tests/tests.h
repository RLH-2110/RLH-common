#ifndef included_tests_h
#define included_tests_h

/* echos the string, expect if the input is NULL or an empty string. NULL will be replaced with the "NULL" as text, and empty strings get replaced with "(EMPTY)"*/
char* got(char* str);

/* included_tests_h */
#endif